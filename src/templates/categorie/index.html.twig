{% extends 'base.html.twig' %}

{% block title %} Liste des annonces de la catégorie {{ category.name }}{% endblock %}

{% block body %}
    <main class="container">
        <div class="col-12">
            <h1> Catégorie {{ category.name }} </h1>
        </div>
<<<<<<< HEAD
        <div class="col-12 d-flex flex-row flex-wrap justify-content-center gap-5">
=======

        <div class="col-12 d-flex flex-row justify-content-center gap-5">
>>>>>>> 70ae5bf49de58213136054d1832dbc312dc3d932
            {% for articles in article %}
                <div class="d-flex flex-column">
                    <div>
                        <img src="{{ images.name }}">
                    </div>
                    <div class="d-flex flex-column">
                        <h4><a href="{{ path('detailarticle', {id: articles.id}) }}">{{ articles.name }} </a></h4>
                        <p> {{ articles.price }} €</p>
                    </div>
                    <a class="redir" href="#"></a>

                </div>
            {% endfor %}
        </div>
        {{ dump() }}

    </main>
{% endblock %}
